# graphql-apollo-express-api-ts

Boilerplate for simple GraphQL Server with Apollo Server and Express.

Apollo Sandbox: http://localhost:3000/graphql

```bash
query {
  helloWorld
}
```

## Running the app

```bash
# watch mode
$ npm run start:dev

```
